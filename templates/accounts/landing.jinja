{% extends "base/index.jinja" %}

{% block title %}Welcome to UniHub{% endblock %}

{% block content %}
<div class="hero min-h-screen bg-base-200">
  <div class="hero-content text-center">
    <div class="max-w-md">
      <h1 class="text-5xl font-bold">UniHub</h1>
      <p class="py-6">Connect with your university community, join groups, attend events, and more!</p>
      <div class="flex justify-center gap-4">
        <a href="{{ url('login') }}" class="btn btn-primary">Login</a>
        <a href="{{ url('signup') }}" class="btn btn-outline">Sign Up</a>
      </div>
    </div>
  </div>
</div>
{% endblock %} 