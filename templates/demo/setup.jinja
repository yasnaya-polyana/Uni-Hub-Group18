{% extends 'base/index.jinja' %}
{% block title %}Home{% endblock %}
{% block content %}
  <div class="hero min-h-[50vh] bg-base-200 rounded-box mb-8">
    <div class="hero-content text-center">
      <div class="max-w-md">
        <h1 class="text-5xl font-bold">Welcome to UniHub Demo</h1>
        <p class="py-6">Connect with your university community, join groups, and participate in events.</p>
        {% if not false %}
          <a href="{{ url('demo-init') }}" class="btn btn-primary btn-lg">Initialise Demo Data</a>
          <a href="{{ url('home') }}" class="btn btn-outline ml-2">Go Home</a>
        {% endif %}
      </div>
    </div>
  </div>

{% endblock %}
