<a href="{{ url('community_detail', community.id) }}" class="block">
	<div class="card bg-base-200 hover:bg-base-300 transition-colors">
		<div class="card-body p-4">
			<h3 class="card-title text-sm">{{ community.name }}</h3>
			<p class="text-xs text-base-content/60">{{ community.members.count() }} members</p>
		</div>
	</div>
</a>