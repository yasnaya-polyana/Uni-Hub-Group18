<div class="flex space-x-2 items-center justify-center">
    {% include 'posts/profile-picture.jinja' %}
    <a class="text-sm font-bold" title="@{{ (post|translate_repost).user.username }}" href="{{ url("user", post.user.username) }}">
        {{ (post|translate_repost).user.first_name }} {{ (post|translate_repost).user.last_name }}
        <span class="font-light text-xs">@{{ post.user.username }} </span>
    </a>
</div>

